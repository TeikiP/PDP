#include "island.h"
#include <unistd.h>
#include <stdio.h>
#include <stdlib.h>

#define INT_TO_CHAR 48 /* constant to translate an unsigned into a char*/

/* Generate a file containing the current state of the grid */
void generate_game_file(Grid* grid,
                        unsigned size_grid,
                        unsigned nb_island,
                        char* g_value);

/* Checks the absolute distance between two integers. */
unsigned distance(unsigned a, unsigned b);